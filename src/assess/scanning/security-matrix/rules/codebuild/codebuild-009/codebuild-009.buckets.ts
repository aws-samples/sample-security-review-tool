/**
 * Placeholder name for the artifact bucket that the pipeline service manages on
 * the project's behalf. The bucket's real name is not declared in the template,
 * so this stand-in is used when reporting and when matching wildcard grants.
 */
export const PIPELINE_ARTIFACT_BUCKET = 'the pipeline-managed artifact bucket';
