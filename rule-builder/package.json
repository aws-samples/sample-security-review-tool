{
  "name": "rule-builder",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Generates requirements, implements rule logic, and produces tests for security-matrix rules.",
  "scripts": {
    "implement": "bun src/index.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.200.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "@types/node": "^22.16.5",
    "aws-cdk": "^2.200.0"
  }
}
